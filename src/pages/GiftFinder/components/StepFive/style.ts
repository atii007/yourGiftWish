import { colors } from "src/theme/colors";

export const stepFiveStyle = {
    mainBox : {
        border: `1px solid ${colors.primary.main}`,
        p: 7,
        textAlign: "center",
        borderRadius: "10px",
    }
    
}